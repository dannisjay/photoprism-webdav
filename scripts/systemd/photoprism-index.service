[Unit]
Description=PhotoPrism Local 定时增量索引
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
WorkingDirectory=/opt/photoprism-1.0
ExecStart=/usr/bin/docker compose exec -T photoprism photoprism index